{
  "name": "plan-financiero-sync-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check worker.js",
    "test": "node ../tools/verify-sync-worker.mjs",
    "deploy": "wrangler deploy"
  }
}
